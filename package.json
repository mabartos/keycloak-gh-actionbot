{
  "name": "keycloak-bot",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "dist": "npx -p @vercel/ncc ncc build index.js",
    "format": "prettier --write ."
  },
  "type": "module",
  "dependencies": {
    "@actions/core": "^1.2.0",
    "@actions/github": "^5.0.3",
    "octokit": "^2.0.7"
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "prettier-cli": "^0.1.0"
  }
}
